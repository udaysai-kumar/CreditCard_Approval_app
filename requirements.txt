# AI Credit Card Approval Prediction System — dependencies
Flask>=2.3,<4.0
Werkzeug>=2.3,<4.0
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
joblib>=1.3
gunicorn>=21.0

# Optional: if you have XGBoost available on your platform, train_model.py
# can be extended to use it directly (a GradientBoostingClassifier stand-in
# is used by default since XGBoost isn't part of the Python standard
# scientific stack and may not be installable in all environments).
# xgboost>=2.0
